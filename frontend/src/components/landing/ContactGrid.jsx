const fallback = [{ name: "Administrador", role: "Dirección del estudio", email: "contacto@anothergamemore.com", initials: "A" }, { name: "Game Design", role: "Diseño y producto", email: "design@anothergamemore.com", initials: "G" }, { name: "Community", role: "Comunidad y alianzas", email: "community@anothergamemore.com", initials: "C" }];
export default function ContactGrid({ contacts = fallback }) {
  return <section className="contact-section" id="equipo" data-reveal><span className="section-kicker">Nuestro equipo</span><h2>Contacto</h2><p className="section-lead">Las personas detrás de cada experiencia. Encuentra el contacto correcto para cada tema.</p><div className="contact-grid" id="contacto">{contacts.map((contact, index) => <article className="contact-card" style={{ "--card-index": index }} key={contact.id || contact.email || index}><div className="initials">{contact.initials || contact.name?.slice(0, 1)}</div><h3>{contact.name}</h3><p>{contact.role}</p>{contact.email && <a href={`mailto:${contact.email}`}>{contact.email}</a>}{contact.discord && <small>{contact.discord}</small>}</article>)}</div></section>;
}
